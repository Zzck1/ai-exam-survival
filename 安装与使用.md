# AI期末保命速学：安装与使用

本包是由AI执行的规则型 Skill，不是独立应用。运行只需要平台能读取 Markdown 规则、读取学生课程资料并输出 Markdown；不需要 API Key、联网、数据库或脚本依赖。

## 文件说明

- [SKILL.md](SKILL.md)：技能入口、触发条件及阶段顺序。
- `references/`：一次问诊、资料门槛、评分、课程策略、时间压缩、最终检查。
- `templates/`：阻断报告、输出结构、全部通用文件和三种04专属模板。

把整个 `ai-exam-survival` 文件夹一起迁移，保留相对路径；不要只复制入口丢掉规则和模板。学生课程资料单独存放，不要混入Skill目录。

## 在Codex使用

当前机器的个人技能目录使用 `C:\Users\28134\.codex\skills`，本包可放在其 `ai-exam-survival` 子目录。其他版本按实际技能目录安装；当前官方文档列出用户级 `~/.agents/skills/ai-exam-survival/` 和项目级 `.agents/skills/ai-exam-survival/`。只选择一个实际被发现的位置，避免同名重复。

安装后在技能选择器中找到 `ai-exam-survival`，或在请求中使用 `$ai-exam-survival`；若列表尚未出现，重启Codex。也可直接指定本包的 `SKILL.md` 路径请求执行。技能结构、发现目录和显式调用方式参考 [OpenAI官方技能文档](https://learn.chatgpt.com/docs/build-skills)（2026-09-17核对）。

不要把这里的绝对路径当作其他设备固定路径，也不要改动其他已安装技能。重复安装前保留自己的旧版本。

## 在其他平台迁移

ChatGPT Work、WorkBuddy、TraeWork及其他支持Skill或规则文件的环境，可使用其实际提供的导入/规则加载能力装入整包；本包不假设它们拥有相同按钮、目录或文件API。若没有原生Skill机制，在当前任务提供入口、全部引用规则、共用模板及对应课程模板，明确要求遵循入口并读取引用文件。

必须能完整读取核心课程资料并保留核验所需上下文。不支持某种文件时先由学生转为清晰、完整的可读格式；只检索到片段不算读完。不支持批量写文件时，Skill按编号给出11份分别标注文件名的Markdown内容。容量仍不足则输出缺失报告，不承诺伪完整包。

以上是移植方式，不表示已经在各平台做过兼容性实测；本次行为验证范围见交付的验收报告。

## 示例调用

先把教材、老师复习资料、日常课件、Tutorial/作业及答案、模拟/往年题分别规范命名，放在同一课程资料文件夹。缺往年题可继续，但必须有基本覆盖范围的教学材料和可靠带答案题目。

```text
$ai-exam-survival
请用我提供的“课程资料”文件夹生成期末保命学习包。
课程：管理学
考试：2026-09-24 09:00，Asia/Shanghai
每天可学习：3小时
考试方式：闭卷
题型及分值：不清楚
满分及及格线：100分，60分
当前基础：零基础
请开始；仅使用上传资料。通过体检后一次生成全部独立Markdown文件。
```

信息没有一次填全时，Skill只问一次缺失字段，允许回答“不清楚”。不知道及格线/日期等会保留不确定性，不给成绩保证。

正常结果是 `课程名_期末保命学习包/` 下00—10共11个文件。先读00，再按02完成任务；做题后用07核对，考前看09，重大缺失/冲突看10。若只收到 `资料读取与缺失报告.md`，按其中具体要求补资料，再要求“沿用之前信息，重新体检并完整重生成”。

## 本包不做什么

课程知识不联网补充、不凭模型记忆补全；时间不足自动缩小范围，不保证及格。测试用虚构课程只用于开发验收，不能作为真实课程依据。学生每次生成仍需执行完整质量检查。

